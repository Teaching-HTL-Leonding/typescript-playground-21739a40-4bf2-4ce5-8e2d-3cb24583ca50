let length1 = 20;

function setup() {
    createCanvas(400, 400);
    background("black");
    stroke("lime    ");

    for(let i = 0; i < width; i += length1 ){
        line(i  , i, width, i);
        line(SIZE)
    }
}

function draw() {
}
